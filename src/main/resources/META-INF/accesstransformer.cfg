# Widen AbstractContainerScreen#hoveredSlot to public so ItemLookup can read it
# directly instead of via runtime reflection (ObfuscationReflectionHelper), which
# broke on modpacks running a different Forge patch version than our dev pin.
public net.minecraft.client.gui.screens.inventory.AbstractContainerScreen f_97734_ # hoveredSlot
